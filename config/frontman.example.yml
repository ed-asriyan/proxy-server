# public domain of frontman (with instruction html and config json files)
config_frontman_domain:

# port users should connect to for instructions and json configs by https
config_frontman_port:

# where redirect to if user opened index page without valid parameters
config_frontman_default_redirect:

# email for certbot verification
config_frontman_certbot_email:
