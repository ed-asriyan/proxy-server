{% for user in users.keys() %}
{{ loop.index }}. {{ user }}:
{% for secret in users[user] %}
  {{ loop.index }}. `ss://{{ (cipher + ':' + secret) | b64encode }}@{{ domain }}:{{ ss_port }}`
{% endfor %}

{% endfor %}
